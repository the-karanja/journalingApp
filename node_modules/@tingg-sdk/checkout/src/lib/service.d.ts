/**
 * Created by Kelvin Macharia on 12/03/2024
 */
import { Environment, Payload } from "./types";
import '../fetch-polyfill';
declare const services: {
    fetchToken: (apiKey: string, clientId: string, clientSecret: string, environment?: Environment) => Promise<Response>;
    expressRequest: (accessToken: string, payload: Partial<Payload>, environment?: Environment) => Promise<Response>;
};
export default services;
